<Page
    x:Class="MicroWinUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Icon="OpenFile" Label="打开" Click="OpenButton_Click"/>
            <AppBarButton Icon="Save" Label="保存" Click="SaveButton_Click"/>
        </CommandBar>

        <InkToolbar TargetInkCanvas="{x:Bind inkCanvas}" Grid.Row="1" />

        <Grid Grid.Row="2" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" ZoomMode="Enabled" MinZoomFactor="0.1" MaxZoomFactor="5.0">
                <Grid x:Name="ImageGrid" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image x:Name="DisplayImage" Stretch="Fill" UseLayoutRounding="True" />
                    <InkCanvas x:Name="inkCanvas" />
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
