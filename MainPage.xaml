<Page
    x:Class="MicroWinUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:local="using:MicroWinUI"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar DefaultLabelPosition="Right">
            <AppBarButton Icon="OpenFile" Label="打开" Click="OpenButton_Click"/>
            <AppBarButton x:Name="SaveButton" Icon="Save" Label="保存" Click="SaveButton_Click" IsEnabled="False"/>
        </CommandBar>

        <InkToolbar x:Name="MainInkToolbar" TargetInkCanvas="{x:Bind inkCanvas}" Grid.Row="1" ActiveToolChanged="InkToolbar_ActiveToolChanged" IsEnabled="False">
            <InkToolbarCustomToggleButton x:Name="HandToolButton" Click="HandToolButton_Click" ToolTipService.ToolTip="抓手工具" IsEnabled="{x:Bind IsCheckedNegation(HandToolButton.IsChecked), Mode=OneWay}">
                <FontIcon Glyph="&#xE7C9;"/>
            </InkToolbarCustomToggleButton>
            <InkToolbarCustomToggleButton x:Name="CropButton" Click="CropButton_Click" ToolTipService.ToolTip="裁剪" IsEnabled="{x:Bind IsCheckedNegation(CropButton.IsChecked), Mode=OneWay}">
                <FontIcon Glyph="&#xE7A8;"/>
            </InkToolbarCustomToggleButton>
        </InkToolbar>

        <Grid Grid.Row="2" Background="{ThemeResource LayerOnAcrylicFillColorDefaultBrush}">
            <ScrollViewer x:Name="MainScrollViewer" 
                          HorizontalScrollBarVisibility="Auto" 
                          VerticalScrollBarVisibility="Auto" 
                          ZoomMode="Enabled" 
                          MinZoomFactor="0.1" 
                          MaxZoomFactor="5.0"
                          PointerPressed="MainScrollViewer_PointerPressed"
                          PointerMoved="MainScrollViewer_PointerMoved"
                          PointerReleased="MainScrollViewer_PointerReleased"
                          PointerCanceled="MainScrollViewer_PointerReleased">
                <Grid x:Name="ImageGrid" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image x:Name="DisplayImage" Stretch="Fill" UseLayoutRounding="True" />
                    <InkCanvas x:Name="inkCanvas" />
                    <local:CropControl x:Name="cropControl" Visibility="Collapsed" CropConfirmed="CropControl_CropConfirmed" CropCancelled="CropControl_CropCancelled" />
                </Grid>
            </ScrollViewer>

            <StackPanel x:Name="CropButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,30" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" Padding="10" CornerRadius="8" Visibility="Collapsed">
                <Button Content="确认裁剪" Click="ConfirmCrop_Click" Style="{ThemeResource AccentButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="取消" Click="CancelCrop_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
