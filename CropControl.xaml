<UserControl
    x:Class="MicroWinUI.CropControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicroWinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Style x:Key="CropThumbStyle" TargetType="Thumb">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Fill="White" Stroke="{ThemeResource SystemControlHighlightAccentBrush}" StrokeThickness="2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="Transparent" PointerPressed="LayoutRoot_PointerPressed">
        <!-- 半透明遮罩层 -->
        <!-- 4-Rectangle Mask Layer -->
        <Canvas x:Name="MaskCanvas"
                Background="Transparent"
                PointerPressed="MaskCanvas_PointerPressed"
                PointerMoved="MaskCanvas_PointerMoved"
                PointerReleased="MaskCanvas_PointerReleased"
                PointerCanceled="MaskCanvas_PointerReleased"
                PointerEntered="MaskCanvas_PointerEntered"
                PointerExited="MaskCanvas_PointerExited">
            <Rectangle x:Name="TopMask" Fill="#66000000" IsHitTestVisible="False" />
            <Rectangle x:Name="BottomMask" Fill="#66000000" IsHitTestVisible="False" />
            <Rectangle x:Name="LeftMask" Fill="#66000000" IsHitTestVisible="False" />
            <Rectangle x:Name="RightMask" Fill="#66000000" IsHitTestVisible="False" />
        </Canvas>

        <!-- 裁剪对齐网格线 (可选) -->
        <Canvas x:Name="SelectionCanvas" IsHitTestVisible="False">
            <!-- 裁剪框主体，支持拖动平移 -->
            <Border x:Name="SelectionBorder"
                    BorderBrush="{ThemeResource SystemControlHighlightAccentBrush}"
                    BorderThickness="2"
                    Width="200" Height="200"
                    Background="Transparent"
                    Canvas.Left="50" Canvas.Top="50"
                    IsHitTestVisible="True"
                    PointerPressed="SelectionBorder_PointerPressed"
                    PointerMoved="SelectionBorder_PointerMoved"
                    PointerReleased="SelectionBorder_PointerReleased"
                    PointerCanceled="SelectionBorder_PointerReleased">
                
                <Grid>
                    <!-- 3x3 辅助线 -->
                    <Grid Opacity="0.5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Column="0" Grid.RowSpan="3" BorderBrush="White" BorderThickness="0,0,1,0"/>
                        <Border Grid.Column="1" Grid.RowSpan="3" BorderBrush="White" BorderThickness="0,0,1,0"/>
                        <Border Grid.Row="0" Grid.ColumnSpan="3" BorderBrush="White" BorderThickness="0,0,0,1"/>
                        <Border Grid.Row="1" Grid.ColumnSpan="3" BorderBrush="White" BorderThickness="0,0,0,1"/>
                    </Grid>
                </Grid>
            </Border>

            <!-- 8个拖动手柄 -->
            <!-- Top Left -->
            <Thumb x:Name="ThumbTL" Style="{StaticResource CropThumbStyle}" Canvas.Left="40" Canvas.Top="40" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="TL" IsHitTestVisible="True"/>
            <!-- Top -->
            <Thumb x:Name="ThumbT" Style="{StaticResource CropThumbStyle}" Canvas.Left="140" Canvas.Top="40" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="T" IsHitTestVisible="True"/>
            <!-- Top Right -->
            <Thumb x:Name="ThumbTR" Style="{StaticResource CropThumbStyle}" Canvas.Left="240" Canvas.Top="40" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="TR" IsHitTestVisible="True"/>
            <!-- Right -->
            <Thumb x:Name="ThumbR" Style="{StaticResource CropThumbStyle}" Canvas.Left="240" Canvas.Top="140" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="R" IsHitTestVisible="True"/>
            <!-- Bottom Right -->
            <Thumb x:Name="ThumbBR" Style="{StaticResource CropThumbStyle}" Canvas.Left="240" Canvas.Top="240" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="BR" IsHitTestVisible="True"/>
            <!-- Bottom -->
            <Thumb x:Name="ThumbB" Style="{StaticResource CropThumbStyle}" Canvas.Left="140" Canvas.Top="240" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="B" IsHitTestVisible="True"/>
            <!-- Bottom Left -->
            <Thumb x:Name="ThumbBL" Style="{StaticResource CropThumbStyle}" Canvas.Left="40" Canvas.Top="240" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="BL" IsHitTestVisible="True"/>
            <!-- Left -->
            <Thumb x:Name="ThumbL" Style="{StaticResource CropThumbStyle}" Canvas.Left="40" Canvas.Top="140" DragStarted="Thumb_DragStarted" DragDelta="Thumb_DragDelta" Tag="L" IsHitTestVisible="True"/>
            
        </Canvas>

    </Grid>
</UserControl>
